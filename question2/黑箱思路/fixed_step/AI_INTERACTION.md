# 固定步长优化方案 · AI 交互记录

更新日期：2026-09-11。本目录用于交接，实际优化代码位于 `../symmetry/train.py`。

## 用户需求与历史更正

用户问 750/375 是否已经优化。首版没有优化，是手设基准；原先报告的约 23% 改善不能解释为胜过最优固定策略。该更正已保留在报告中。

## 已执行方案

把所有状态共享的 `(a,|b|)` 作为两个可训练参数，从 4 个初值出发，在训练分布上直接最小化平均剩余面积；用独立验证集选检查点。与神经网络共享批次以便公平比较，但不共享参数。

- 训练种子 21260911；验证种子 21360911。
- 最终验证集选择：前进 **931.5303 m**，侧移绝对值 **277.7553 m**。
- 新独立测试种子 23260911：固定优化策略 **546.1169 m²**；手设 750/375 为 **657.9780 m²**；共享分支网络双侧平均 **505.2302 m²**。
- 新网络相对优化常数约改善 7.49%。

## 证据

`../results/symmetry/optimized_fixed.json`、`evaluation.json`、`fixed_landscape.npz`；图和解释见 [补充报告](../symmetry/README.md)。

```powershell
python -m question2.黑箱思路.symmetry.train
python -m question2.黑箱思路.symmetry.evaluate
python -m question2.黑箱思路.symmetry.diagnostics
```

## 限制

这是特定先验与初测点分布下的数值优化，不是证明全局最优的通用常数。左右在总体对称分布下可交换，但同一非对称边界状态未必等价。此方案没有使用测试集选参数。
