# 黑箱思路 · AI 交互记录

## 最新状态：半径接近20与正交引导

2026-09-11：按最新用户要求在 [radius20](radius20/AI_INTERACTION.md) 重写目标、训练和评估，默认run.ps1已切到新方案，旧面积用 `-Objective area`。六模型各1800步；无引导MAE20=5.15709 m、正交引导5.16951 m。107项测试通过，九张R1800全局/局部图与统计完成。用户追问NN点输出不能表示可行域，已增加候选集合策略、独立复核和保守几何候选内集。旧数据以下保留历史含义。

更新日期：2026-09-11。

## 用户需求与公开决策

用户要求三参数策略网络，以二次交会面积期望最小训练，并要求满足题意。决定使用正确条件后验、同源固定接收半径、同坐标固定误差、失联保留首轮面积和 near 后光学定位分支。

公开输入三参数；内部利用旋转对称性变换为两个局部状态量。圆域用外包络评分，抽样用真实圆；纯交会多边形和圆裁切区域分开解释。

## 已完成与证据

- 4 网络各训练 6000 步，约 85.32 秒，权重 `checkpoints/best.pt`。
- 8192 状态 × 256 条件样本的独立评价：网络 503.42 m²，接收认证网络 503.68 m²，后验矩近似公式 502.76 m²，手设 750/375 基准 655.25 m²。
- 首版最终联合测试 80 项通过，见 `results/verification.json`。
- [完整报告](分析报告.md)、[运行说明](README.md)、`results/evaluation.json`、`results/candidates/` 保存全部证据。

## 必须保留的限制和更正

- 目标先验、误差分布和半径分布为假设；不是官方模拟器分布。
- 后验矩公式是近似模型的解；没有完整问题的闭式最优性证明。
- 固定 750/375 未优化；不能以此宣称网络胜过所有固定策略。
- 第一测点仅在圆内训练；边缘向外案例中首版 NN 约 32.66 m²、网格约 27.80 m²。
- 半径固定为 1000 m 的压力测试中，网络曾输给手设固定策略。
- `safe_pi` 保证收到信号或 near，不保证总能返回方向或两次观测后即可清除。

## 新增需求与当前工作

用户要求对称双分支、外部 bool 选边、优化固定步长、固定真值热力图和网络函数关系。扩展已完成，文件在 [symmetry](symmetry/AI_INTERACTION.md)，保留首版文件和权重供对照。独立基准另有 [解析方案记录](analytic/AI_INTERACTION.md) 和 [固定步长记录](fixed_step/AI_INTERACTION.md)。当前联合测试为 84 项通过，先前 80 项是首版完成时的历史结果。

用户进一步询问无标签训练。已说明当前实现通过可微几何损失反传，若只有标量黑箱分数则需策略梯度/演化/有限差分；直径可替换目标，但尚未改训。

2026-09-11 再次追问补充：见 [逐步训练与数学模型](../数值优化思路/训练与数学模型.md)。二维选点可以不使用网络；直接局部优化已运行。此前“可微”应限定为分段可微，near 硬判定可能漏掉期望的移动边界导数，不能声称完整目标拥有无偏路径梯度。本次仅新增说明和独立示例，未修改训练模型。

## 复现

```powershell
& './question2/黑箱思路/run.ps1' -Task all -Python 'D:\ProgramData\anaconda3\python.exe'
```

若只查看首版结果，直接读已保存文件即可，不必重新训练。新对称方案命令见其记录。
