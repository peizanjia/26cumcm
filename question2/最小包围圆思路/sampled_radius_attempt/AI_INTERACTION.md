# 历史尝试：直接抽样接收半径进行引导训练

2026-09-11。父方案的第一次尝试，不是当前最终权重。固定数据中显式抽样 R，MEC 评分随动作跨过接收阈值会跳变；强超标平方惩罚的有限差分更新不稳定，验证最优引导模型停留在旧面积初始化。

该目录保留当时两模型权重、training.csv、evaluation.json、statistics.csv 和 evaluation_samples.npz。纯半径模型平均 23.41343 m、最大 750.11425 m，引导模型等同旧面积模型平均 28.05464 m。未隐藏失败；父方案随后解析边缘化 R 后训练成功改善。当前结果和复现代码见 [上级记录](../AI_INTERACTION.md)。该历史分支的具体替换点是 `cost(evaluate(d,q),q,guided)`，当前代码改用 `expected_cost(d,q,guided)`；历史数据保留，但没有单独冻结一套源代码。
