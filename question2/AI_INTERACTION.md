# 问题 2 · AI 交互记录

更新日期：2026-09-11。

## 用户目标

首轮需求：训练 `pi(x,y,theta)->(x',y')`，以两次测向后的期望面积为目标；正确引入目标、测点、方向误差等随机性；建立黑箱思路子目录并高效并行执行。

追加需求：厘清解析公式、圆边界裁切误差；利用对称性改网络并将选边作为第三、四问全局地图的 bool 决策；优化固定步长；画固定首测点和真实目标的得分热力图；研究网络是否相当于固定步长或显式函数。

## 方案入口

- **最新要求**：[接近20与正交引导](黑箱思路/radius20/AI_INTERACTION.md)：训练六模型、统计、R1800全局/局部图；NN候选与可行域分离，保守几何候选内集。

- **当前指标**：[最小包围圆半径](最小包围圆思路/AI_INTERACTION.md)：新训练、20 m 可行域、线性热力图、分布统计与约束最短移动。

- [黑箱首版记录](黑箱思路/AI_INTERACTION.md)：已有训练和测试。
- [对称方案记录](黑箱思路/symmetry/AI_INTERACTION.md)：当前扩展工作。
- [解析近似记录](黑箱思路/analytic/AI_INTERACTION.md)。
- [固定步长优化记录](黑箱思路/fixed_step/AI_INTERACTION.md)。
- [不使用 NN 的数值优化记录](数值优化思路/AI_INTERACTION.md)：均匀条件模型、可微链式法则、实际单状态优化示例。

## 已确认的重要更正

- 题目只给半径和误差的取值界，没有规定均匀分布；均匀是额外假设。
- 首次有效接收使目标后验受到接收半径存活函数加权，不是整个扇形均匀。
- 所谓“解析解”是小角度近似目标的闭式解，不是完整裁切/失联问题的全局闭式解。
- 750/375 m 是首版手设基准，未优化；23.2% 改善不能解读为胜过最优固定步长。
- 同一不对称状态左右动作不一定同分；严格反射对称性要求状态和动作一起镜射。

## 状态

首版和扩展均完成。新独立测试：对称双分支平均 505.230 m²，优化固定步长 546.117 m²，后验矩公式 505.361 m²。84 项联合测试通过。所有数据为本地合成实验，尚未进行官方演练。完整新报告为 `黑箱思路/symmetry/README.md`。

## 追加需求：训练原理与第一问直径

用户询问无监督标签时如何训练策略映射，并提出可把第一问直径用作指标。当前采用可微模拟器路径梯度，非监督标签训练、非纯黑箱策略梯度；面积权重尚未改训成直径权重。具体说明已写入对称方案报告第 1 节。

再次追问后补充完整数学模型及无 NN 示例，见 `数值优化思路/训练与数学模型.md`。更正：几何分段可微，near 硬切换不能保证普通路径梯度正确。初始先验均匀不推出接收后的后验均匀。直接局部优化确已运行，但没有连续圆/积分/全局最优的精确认证。

## 当前变更：半径取代面积

用户明确改为最小包围圆半径，非直径/2。新方案完成，联合测试 99 项通过。半径 NN 均值 23.34594 m、样本最大 750.11425 m；门槛/距离引导 NN 均值 23.50726 m、样本最大 55.26477 m、达标率 41.26%。统计含极值、方差、标准差、分位数和距离。网格可行域区分均值/95%机会/样本最大，独立复核失败如实记录。旧分析的面积最优公式不能直接移植成半径最优公式。

## 最新变更：接近20、正交引导和集合策略

本轮明确采用E|rho-20|，不是半径越小越好。六模型1800步完成，独立2048状态×256事件×双侧等权；无引导MAE20=5.15709，引导=5.16951，107项测试通过。引导方向更正交但主指标略差。大半径失联尾部保留统计，不声称达标保证。未知目标可行域与固定真值热力图分开；新增所有角区间覆盖的保守上界候选内集，浮点实现非连续域证书。最新报告 `黑箱思路/radius20/README.md`。



## 2026-09-13 问题二核心方案代码交付

用户要求将问题二附录代码与支撑材料中的核心方案代码推送给协作者。沿用 `question2/minimax`，将本地边界感知 Minimax MEC 版本整理为可复现模块，并提供独立附录代码、正式数据和论文源码。原有直径/NN对比结果保留为历史，不作当前指标依据。执行状态、验证和限制见[本次交接记录](minimax/AI_INTERACTION.md)。
