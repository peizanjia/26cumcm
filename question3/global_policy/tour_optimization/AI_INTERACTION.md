# 第三问：共路策略继续优化

## 官方练习连接主程序（2026-09-13）
- 用户要求将已推送最终策略连接附件中的模拟器，并支持手动开始一次、脚本运行一次、结束后不退出继续下一次。附件确认接口只有 `/enter`、`/measure`、`/clear`、`/exit`；开始测试按钮没有HTTP调用方式。
- 新增 `official_main.py`。它使用 `best_parameters.json` 创建最终 `tour` Planner，`OfficialHTTPClient` 对 `/enter` 使用同一个request_id持续轮询，HTTP连接错误重试但不重复生成动作ID；收到accepted=true后由Planner执行全部公开动作。每局保存summary、commands.jsonl、http_requests.jsonl（完整请求/响应）、decisions、map和配置到独立时间戳目录。
- 新增 `question3/main.py` 薄入口，推荐从项目根目录用 `python -m question3.main` 启动。
- 主循环一局结束后返回下一次`/enter`等待，`--once`可只运行一局，Ctrl+C在活动局尽量发送`/exit`。`--launch-simulator --simulator-exe`可先启动已解压模拟器，但不能代替界面开始点击。
- 新增 `test_official_main.py`，用本地临时HTTP服务器验证：开始轮询两次且复用request_id；动作payload包含default arena、位置和频道并使用新的request_id；请求历史可保存。2项测试通过；另用本地模拟器完成一次HTTP端到端联调（16/16，184.5881s/源），不计入策略统计、未连接官方服务、未进行官方练习。
## 本轮提交与推送（2026-09-12）
- 用户要求确认交互记录已同步后push到remote。已核查根目录、第三问、全局策略及本方案记录一致，准备提交本轮第三问源码、参数搜索/验证证据、报告与离线诊断到origin/main。第四问及其文献并行修改不纳入本次提交。
- 沿用已完成的105项策略测试、100场最终验证和新增下界诊断核验；提交前检查Git差异。通过既有本机代理完成origin读取，未改全局Git配置；实际提交与推送结果随后记录。
- 实际结果：ab23069（feat(q3): optimize joint tours with Bayesian tuning and validation）已成功推送origin/main，远端由f133a6c前进至ab23069，共2983个第三问相关文件。提交前仅清理生成SVG的行尾空白，Git暂存差异检查通过；不改变策略和验证成绩。运行缓存未提交，第四问和其文献工作保持在本地。本条成功记录通过后续文档提交同步。

## 当前状态（2026-09-12）
本轮结构优化、昂贵参数搜索、独立验证与报告均已完成。最终100新场景最佳242.3597s/源，上一版同场257.1196，改善5.7405%；各1302/1302源。105项测试通过，没有遗留本轮Python计算进程。目标200尚未达到，差42.3597s/源。当前使用best_parameters.json与run.py；详见README.md。下方“待运行”等属于阶段历史，不代表当前状态。

## 用户需求（2026-09-12）
- 在250.9197s/源的已推送tour基础上继续优化，先根据轨迹和建模分析改进；结构改进不足时再针对昂贵、多场景参数评估选择合适优化算法。按顺序执行，最后给出本轮最佳已验证结果。最终仍以平均200以下为目标。
- 仅本地合成，原官方测试限制继续有效。保留已推送策略、旧种子和原始证据。新的验证场景在候选冻结后使用。

## 阶段与建模决定
- 第一阶段：覆盖节点坐标优化（保持预测覆盖并集的连续约束），开放路线次序优化，按近期预期覆盖延后重复扫频，局部观测兼顾其他源。分别比较，不把计划写成收益。
- 第二阶段：仅调当前实际生效的行为参数；旧search_space中扇区/外环参数对tour不生效，不能盲目套用。每组在多场景上评估，采用共同场景降低配对噪声；筛选与最终验证分开。
- 第三阶段：冻结候选及源代码，至少100个新场景与上一版tour配对测试，保留失败/退步案例及可复现配置。
- 初始阶段快照：开始实现第一阶段，当时尚无本轮新成绩。

## 已执行与更正
- 结构消融14场×6组全部完成：previous271.3191；refine262.7548；exact271.4699；defer270.8919；coupled271.4833；refine_exact257.7187s/源。不能将此开发均值与上轮250.9197直接相减。
- 结构独立验证20273100–20273199：previous253.7583、refine_exact243.7824，差−9.9759、95%区间[−12.4836,−7.4682]，82/100更快，各1292/1292源。首次误启动了6组验证后取消，留下outputs/structure_validation不完整输出，仅最终outputs/validation_100完整两组计入统计。
- 初步参数试验为8组拉丁超立方+2组确定性邻域，每组14场20273200–20273213。最优local_01开发238.4254s/源；第一次独立复核20273300–20273399：previous251.3524、optimized241.7930，差−9.5594、区间[−12.4593,−6.6595]，73/100更快，各1314/1314源。此批缺未调参结构对照，不能单独证明调参收益。
- 重要更正：初步optimize.py定义了未调用的rbf_predict，报告算法名“RBF-inspired”并不符合实际。实际仅LHS+邻域，已删无效函数、更正文案。旧原报告保留，纠正记录显式指出；初版排名用完成场景均值，10组实际全部完成故结果不变，现改成含失败罚的objective。初版第二轮batch覆盖顶层cases/summary只剩2组，原10组逐场文件仍完整；后续审计按逐场重建。
- 为完成昂贵多场景优化，实施真正Matérn5/2高斯过程+期望改善EI，并结合场景数逐级晋升（14→42→84）。共同种子配对差的方差进入GP噪声。参考Snoek2012与Falkner2018多保真思路，但本实现不是BOHB的KDE算法，也不是证明全局最优。
- 优化7项当前生效参数：未知扫频阈值、建图半径比、建图接收概率、跨源旁扫权重、初始前进距离、建图停点数、回访软罚。未知阈值扩展至.24以检查之前.12边界；固定测量/行动样本精度、物理规则、覆盖证书与完成条件。
- 原10组14场结果作为warm start；再运行5组LHS及12次GP建议。前5名+结构对照扩展两个新14场块；前2名+结构对照扩展42场。最终结果仍待运行，不提前写最优成绩。最后将再使用新的封存场景。
- GP阶段的12次建议已全部完成；共27组参数+1结构对照各14场（其中10组140次直接复用旧结果）。bo_11均值233.7040，结构对照241.6016，配对差−7.8976s/源；bo_10为234.4764。已进入42场晋级阶段，低样本最优不能当最终成绩。bo_11的初始步长落在300米搜索下界，若扩大场景后仍优，应评估是否需边界复核。
- 当前适用测试104项通过，另新增边界10源、仅公开客户端的优化策略测试通过（共5项结构测试），最终合并测试数待最后执行。旧策略源码未改。
- 真实耗时审计：结构独立100场的移动减少8.1442、测量+切频减少1.8413s/源；末源清除后的尾段反而多约0.665，不能说收益来自末尾删除工作。源码快照哈希复核通过，后续仅实验驱动/统计脚本改动已在审计区别于冻结策略。
- 42场晋级中bo_11为230.473、bo_10为231.072、首批pilot_local_01为233.122、结构对照236.803s/源。已按原定规则将bo_11/bo_10和结构对照扩充至84场。
- 搜索边界复核：bo_11初始步长300米触及下界，另测试100/200米与未知门槛.1784/.24的4组组合，同14个开发场景。300米对照233.7040；100米两组234.9561，200米两组234.6255，均未超过已有候选；该批两个未知门槛产生相同行动与成绩，不代表该参数普遍无效。没有继续晋级这4组。参考14场缓存复用，不重复算新增运行。
- 最终适用pytest105项通过，48.74秒；覆盖坐标约束、原策略逐命令一致性、仅公开反馈的边界全清、GP拟合/不确定性/EI、失败参数罚项均已核验。
- 84场决选已完成：结构237.880513、bo_11为231.275735、bo_10为232.029957s/源。bo_11−结构为−6.604778，探索性配对95%区间[−8.695779,−4.513777]，66/84更快；各84场全完成。决定使用bo_11，选择依据与完整配置保存在selection.json/best_parameters.json。
- GP搜索实际新增546场运行，140条warm start复用不重复计算；4组边界复核新增56场。全部搜索对象保持相同内部定位采样精度，以增加场景数而非削减定位精度作多保真预算。
- 最后100新种子20273700–20273799×previous/structural/optimized三组已经启动，尚未生成最终均值；本轮验证不再用于选参数。冻结源码哈希75645a149b545e6f35f6cdb1d80a8f110b7d9cd8ff3d835fcdcaaf8aece773c3，含完整源码ZIP和Python/NumPy/SciPy/Numba版本。
- 最佳配置CLI复跑20273600：11/11源，266.246744s/源；参数、总时间、均值、距离、测量数、清除数、完成状态与84场决选原记录完全一致。单场仅验证可复现性，不作最佳均值。

## 最终完成与交接（2026-09-12，本地合成）
- 100新种子20273700–20273799，previous/structural/optimized三组共300次全部完成，各1302/1302源。均值分别257.1195769、247.0396541、242.3596759s/源；最佳均值95%区间[235.6141884,249.1051634]，P95为298.4727968。
- optimized−previous配对差−14.7599010，95%区间[−18.1532509,−11.3665510]，84/100更快，改善5.7405%。结构节省10.0799227，参数相对结构再省4.6799782，后者95%区间[−7.0930430,−2.2669134]，63/100更快。此次三个对照共同验证，纠正之前两组阶段复核不能拆分参数收益的局限。
- 最佳bo_11：初始前进300m、未知/早期未知扫描门槛0.1784213723、建图半径比0.7386414204、建图接收概率0.7962537774、其他已知源旁扫权重0.5772631289、建图停点7、回访软罚21.98713938s；完整参数在best_parameters.json。先按84场决选写selection.json，再使用最终100场，没有利用这批最终结果重新调参。
- 费用分解：相对上一版每源移动节省10.3632215s、测量3.5780020s、切频0.7274098s、清除0.0912676s。平均路程11.5615降至10.9325km/场，测量151.04降至141.84次/场。最差退步20273722为311.12→333.09s/源，主要多走路，仍保留在诊断报告。
- 全阶段计划内完成1526次完整场景运行（含同场不同参数，非1526个独立世界）；缓存不重复算。其中GP新增546，边界复核新增56，最终验证300。误启动结构批次留下37条完整子结果，排除完整比较；单次CLI核验也不混入均值。执行清单见outputs/execution_inventory.json。
- outputs/release_validation/audit.json校验完整种子、完成率、配对区间、分项费用与源数分层；50文件源码快照SHA256为75645a149b545e6f35f6cdb1d80a8f110b7d9cd8ff3d835fcdcaaf8aece773c3，当前源码逐文件一致。后续仅补充报告/交接元数据。
- 105项相关pytest全部通过（48.74秒），CLI复现逐字段一致。最终HTML选择9个诊断场景、每场3变体；Edge多进度检查无脚本错误、20频道、真值默认隐藏、初始无未来覆盖，图表与截图已人工查看。完整300场原始轨迹与结果保留。
- 已新增参数/规划/路线、批量实验、GP/EI晋级搜索、边界验证、统计审计、报告、CLI及测试模块；文件职责和可复现命令见README.md，建模公式及算法来源见METHODS.md。根、问题、方案索引已同步；第四问及其他协作者工作不覆盖。
- **未完成的长期目标：平均低于200。** 本轮有限预算只选出已评估最佳，没有全局最优或不可达证明。下一步优先补全未知源发现后的全局路线分支，联合生成兼顾多源的连续观测位置；当前旁扫只重排单源候选。未来继续调参后要换新的封存验证种子。

## 参数算法与进一步空间追问（2026-09-12）
- 用户询问哪些参数参与调节、是否靠枚举、论文应如何描述，以及继续降时的空间。已按源码及原始建议核验：6连续+1整数，早期未知门槛与普通门槛绑定；27组参数中包含初步10组、5组新增LHS、12轮真正GP+EI建议。每轮2048 Sobol+512邻域仅评价廉价EI，胜出配置才做昂贵仿真。没有运行原版BOHB、TuRBO或完整Hyperband；论文不得误称。
- 新增PARAMETERS_AND_NEXT_STEPS.md：参数范围/值/方向、SAA目标与失败惩罚、配对噪声、Matérn核、EI、晋级规则、可直接用于论文的方法段落；指出实际三层运动优化SLSQP/Held–Karp或2-opt/Powell。原pilot错误RBF标签继续保留更正，单维因果贡献与等预算优化器比较尚未执行。
- 新增diagnostics/headroom.py，对冻结最终100场进行离线分析，输出outputs/followup_diagnosis.json并记录输入审计SHA256。真实坐标仅供该诊断，不注入规划器。精确最短原点开放中心路线L*，以L*−(40N−20)作清除圆访问移动下界；含成功清除但完全放松观测的均值下界133.6664334s/源。固定当前其余费用时总均值下界200.9349420，说明对这100场仅降移动不足达200；不能把下界差距当可实现收益。
- 平均移动170.0912s/源，占70.181%；未知状态扫描114.03次/场、已知27.81次/场，末源发现发生在平均78.07%时刻。每场23.28次局部决定，5.00次入选动作有正旁扫抵扣，2.87次相对纯单源排名改变（含回访罚，不是旁扫因果效果）。仅结束后覆盖尾段4.8649s/源，不足完成42.3597缺口。
- 验证下界所用Held–Karp在1–7点与全排列独立对照一致，全部100场实际路程/时间高于下界；诊断命令在README链接文档中。未改冻结策略核心、未重跑策略、未新增官方连接，原105项策略测试结论保留且不冒称验证新策略。
- 下一轮建议已按代码缺口明确：跨源价值参与连续候选生成；保证清除点同时优化前后路段；未知扫描估计发现后的路线分支；结构确认后再扩GP预算与有效参数。旁扫数量2和40秒上限目前硬编码，继承字段side_scan_channels未在新局部重排读入，不能盲目调其字段。接收概率.7963接近.8上界可复核，步长100/200米已试差于300米。新方案尚未实施，不写预计收益为结果。
- 本次用户问题的解释、论文说明与诊断已完成。长期200目标未完成，最佳仍242.3597s/源；后续策略验证必须换新封存种子。根/问题/方案索引已同步。
